@page
@model AspNetCore.ListModel
@{
    ViewData["Title"] = "List";
}

<h1>List</h1>

<div>
    <a class="btn btn-lg"
       asp-page="./Add">新增
    </a>
</div>

@foreach (var book in Model.Books)
{
    <partial name="_Summary" model="book" />
};

